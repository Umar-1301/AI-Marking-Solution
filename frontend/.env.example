# Copy this file to .env.
# .env is gitignored and must never be committed.

# No environment variables currently needed — the frontend calls the API via
# relative /api/... paths (proxied to the backend by Vite in dev, and by the
# gateway in production). This file is kept so the standard
# `cp .env.example .env` setup step still applies if vars are added later.
